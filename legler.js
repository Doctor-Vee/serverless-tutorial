export const helloFromDoc = () => {
  return {
    doctorVee: "Hello from the Software Doc",
    age: 29,
    variable: process.env.SAMPLE_ENV_VAR
};

// Just keeping this link here in case you need to see the stacks on AWS
//https://console.aws.amazon.com/cloudformation/home
};
